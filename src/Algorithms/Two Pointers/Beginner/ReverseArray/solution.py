# Reverse Array (TWO POINTERS) - TODO / Template
# Per your instruction, this problem is left for you to solve.
# Use two pointers (left, right) swapping until they meet.

def reverse_array_inplace(arr):
    """
    Reverse the array in-place using two pointers.
    TODO: implement this as practice.
    Suggested approach:
    - left = 0, right = len(arr)-1
    - while left < right: swap arr[left], arr[right]; left+=1; right-=1
    """
    raise NotImplementedError("Implement reverse_array_inplace as exercise.")
